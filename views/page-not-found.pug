extends layout.pug

block content
    article.grid-container.portfolio-intro
        div
        h1= err.status
        p.lead.text-light= err.message
        p= err.stack

    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back